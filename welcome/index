<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LocalBasket | Your Neighborhood, Digitalized</title>
  <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>ðŸ§º</text></svg>">
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    :root {
      --p: #ff8a1a;
      --p-dark: #e56a00;
      --p-soft: #fff7ed;
      --dark: #0f172a;
      --gray: #64748b;
      --white: #ffffff;
      --danger: #ff4757;
      --success: #00d084;
      --radius-xl: 30px;
      --radius-md: 18px;
      --shadow: 0 20px 40px -10px rgba(255, 138, 26, 0.16);
      --shadow-sm: 0 4px 15px rgba(0, 0, 0, 0.05);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Plus Jakarta Sans', sans-serif;
      -webkit-tap-highlight-color: transparent;
    }

    body {
      background: #f8fafc;
      color: var(--dark);
      overflow-x: hidden;
      scroll-behavior: smooth;
    }

    /* --- NAVIGATION --- */
    .nav-container {
      position: sticky;
      top: 0;
      z-index: 1000;
      padding: 15px 25px;
    }

    .navbar {
      max-width: 1300px;
      margin: 0 auto;
      height: 80px;
      background: rgba(255, 255, 255, 0.85);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-radius: var(--radius-xl);
      padding: 0 25px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      box-shadow: 0 4px 30px rgba(0, 0, 0, 0.03);
      border: 1px solid rgba(255, 255, 255, 0.6);
      transition: all 0.3s ease;
    }

    .logo-box {
      display: flex;
      align-items: center;
      gap: 10px;
      text-decoration: none;
    }

    .logo-icon {
      width: 40px;
      height: 40px;
      background: var(--p);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: 800;
      font-size: 20px;
      box-shadow: 0 4px 10px rgba(255, 138, 26, 0.28);
    }

    .logo-text {
      font-size: 24px;
      font-weight: 800;
      color: var(--dark);
      letter-spacing: -1px;
    }

    .nav-actions {
      display: flex;
      align-items: center;
      gap: 15px;
    }

    .sell-btn {
      background: var(--dark);
      color: white;
      padding: 12px 20px;
      border-radius: 16px;
      font-size: 13px;
      font-weight: 700;
      text-decoration: none;
      transition: 0.3s ease;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .sell-btn:hover {
      background: #1e293b;
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(15, 23, 42, 0.2);
    }

    .cart-pill {
      background: var(--p-soft);
      color: var(--p);
      padding: 12px 20px;
      border-radius: 16px;
      font-weight: 800;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 8px;
      transition: 0.3s ease;
    }

    .cart-pill:hover {
      background: var(--p);
      color: white;
      transform: scale(1.05);
    }

    /* --- USER ACCOUNT --- */
    .user-account {
      position: relative;
    }

    .user-profile-trigger {
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 10px;
      border-left: 1px solid #e2e8f0;
      padding-left: 15px;
      margin-left: 5px;
    }

    .user-avatar {
      width: 42px;
      height: 42px;
      border-radius: 14px;
      background: var(--p-soft);
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 800;
      color: var(--p);
      font-size: 16px;
      transition: 0.3s;
    }

    .user-profile-trigger:hover .user-avatar {
      background: var(--p);
      color: white;
    }

    .user-menu {
      position: absolute;
      top: 60px;
      right: 0;
      background: #fff;
      border-radius: 20px;
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
      padding: 20px;
      min-width: 200px;
      z-index: 999;
      border: 1px solid #f1f5f9;
      animation: fadeIn 0.2s ease-out;
    }

    .user-menu p {
      font-weight: 800;
      margin-bottom: 15px;
      font-size: 16px;
      color: var(--dark);
      padding-bottom: 10px;
      border-bottom: 1px solid #f1f5f9;
    }

    .user-menu button {
      width: 100%;
      border: none;
      background: none;
      padding: 10px 12px;
      text-align: left;
      cursor: pointer;
      font-weight: 600;
      color: var(--gray);
      border-radius: 10px;
      transition: 0.2s;
    }

    .user-menu button:hover {
      background: var(--p-soft);
      color: var(--p);
    }

    /* --- HERO --- */
    .hero {
      padding: 100px 20px 80px;
      text-align: center;
      background: radial-gradient(circle at 50% 0%, #f1edff 0%, transparent 60%);
    }

    .hero h1 {
      font-size: clamp(40px, 6vw, 72px);
      font-weight: 800;
      line-height: 1.1;
      letter-spacing: -2px;
      color: var(--dark);
    }

    .hero span {
      color: var(--p);
      background: linear-gradient(135deg, var(--p), #8b5cf6);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .hero p {
      color: var(--gray);
      font-size: 18px;
      margin-top: 20px;
      font-weight: 500;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }

    .search-wrapper {
      max-width: 650px;
      margin: 40px auto;
      display: flex;
      gap: 10px;
      background: white;
      padding: 8px;
      border-radius: 25px;
      box-shadow: var(--shadow);
      border: 1px solid #f1f5f9;
      transition: 0.3s;
    }

    .search-wrapper:focus-within {
      border-color: var(--p);
      transform: translateY(-2px);
    }

    .search-wrapper input {
      flex: 1;
      border: none;
      outline: none;
      padding: 0 20px;
      font-size: 16px;
      font-weight: 600;
      color: var(--dark);
      background: transparent;
    }

    .btn-find {
      background: var(--p);
      color: white;
      border: none;
      padding: 15px 35px;
      border-radius: 18px;
      font-weight: 800;
      cursor: pointer;
      transition: 0.3s;
      font-size: 15px;
    }

    .btn-find:hover {
      background: var(--p-dark);
      box-shadow: 0 10px 20px rgba(255, 138, 26, 0.24);
    }

    /* --- CATEGORY CHIPS --- */
    .categories {
      display: flex;
      justify-content: center;
      gap: 12px;
      margin-bottom: 50px;
      flex-wrap: wrap;
    }

    .chip {
      padding: 10px 24px;
      background: white;
      border-radius: 50px;
      border: 1px solid #e2e8f0;
      font-weight: 700;
      font-size: 14px;
      cursor: pointer;
      transition: 0.3s;
      color: var(--gray);
    }

    .chip.active,
    .chip:hover {
      background: var(--p);
      color: white;
      border-color: var(--p);
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(255, 138, 26, 0.2);
    }

    /* --- MAIN CONTENT & STORES --- */
    .main-content {
      max-width: 1300px;
      margin: 0 auto;
      padding: 0 25px;
    }

    .section-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      margin-bottom: 40px;
    }

    .section-title {
      font-size: 32px;
      font-weight: 800;
      letter-spacing: -1px;
      color: var(--dark);
    }

    .section-subtitle {
      color: var(--gray);
      font-weight: 600;
      margin-top: 5px;
    }

    .map-btn {
      border: none;
      background: var(--p-soft);
      font-weight: 800;
      color: var(--p);
      cursor: pointer;
      font-size: 14px;
      padding: 10px 20px;
      border-radius: 12px;
      transition: 0.3s;
    }

    .map-btn:hover {
      background: var(--p);
      color: white;
    }

    .store-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 30px;
    }

    .store-card {
      background: white;
      border-radius: 28px;
      overflow: hidden;
      border: 1px solid #f1f5f9;
      transition: 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      cursor: pointer;
      position: relative;
    }

    .store-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
      border-color: transparent;
    }

    .store-img-wrap {
      position: relative;
      height: 200px;
      overflow: hidden;
    }

    .store-img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: 0.5s;
    }

    .store-card:hover .store-img {
      transform: scale(1.1);
    }

    .status-badge {
      position: absolute;
      top: 15px;
      right: 15px;
      background: rgba(255, 255, 255, 0.95);
      padding: 6px 14px;
      border-radius: 12px;
      font-size: 11px;
      font-weight: 800;
      color: var(--success);
      backdrop-filter: blur(8px);
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
    }

    .store-body {
      padding: 25px;
    }

    .store-body h3 {
      font-size: 18px;
      font-weight: 800;
      margin-bottom: 5px;
    }

    .store-meta {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-top: 15px;
    }

    .meta-pill {
      background: #f8fafc;
      padding: 6px 12px;
      border-radius: 8px;
      font-size: 12px;
      font-weight: 700;
      color: var(--gray);
      border: 1px solid #edf2f7;
    }

    .empty-state {
      text-align: center;
      color: var(--gray);
      font-weight: 600;
      padding: 80px 0;
      background: #fff;
      border-radius: 20px;
      border: 2px dashed #e2e8f0;
    }

    .sell-badge {
      background: rgba(255, 255, 255, 0.2);
      padding: 4px 10px;
      border-radius: 8px;
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    /* --- AUTH OVERLAY --- */
    .auth-overlay {
      position: fixed;
      inset: 0;
      background: rgba(15, 23, 42, 0.6);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      z-index: 2000;
      display: none;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .auth-card {
      background: white;
      width: 100%;
      max-width: 450px;
      border-radius: 35px;
      padding: 50px;
      position: relative;
      animation: slideUp 0.4s cubic-bezier(0.16, 1, 0.3, 1);
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    }

    @keyframes slideUp {
      from {
        transform: translateY(40px) scale(0.95);
        opacity: 0;
      }
      to {
        transform: translateY(0) scale(1);
        opacity: 1;
      }
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(-10px); }
        to { opacity: 1; transform: translateY(0); }
    }

    .auth-close-btn {
      position: absolute;
      top: 25px;
      right: 25px;
      border: none;
      background: #f1f5f9;
      width: 40px;
      height: 40px;
      border-radius: 50%;
      font-weight: 800;
      font-size: 18px;
      cursor: pointer;
      color: var(--gray);
      transition: 0.2s;
    }

    .auth-close-btn:hover {
      background: #e2e8f0;
      color: var(--dark);
    }

    .auth-tabs {
      display: flex;
      gap: 20px;
      margin-bottom: 30px;
      border-bottom: 2px solid #f1f5f9;
      padding-bottom: 15px;
    }

    .auth-tab-btn {
      border: none;
      background: none;
      font-weight: 800;
      font-size: 20px;
      cursor: pointer;
      color: #cbd5e1;
      padding: 0 0 10px 0;
      position: relative;
      transition: 0.3s;
    }

    .auth-tab-btn.active {
      color: var(--dark);
    }

    .auth-tab-btn.active::after {
      content: '';
      position: absolute;
      bottom: -17px;
      left: 0;
      width: 100%;
      height: 3px;
      background: var(--p);
      border-radius: 10px;
    }

    .form-group input {
      width: 100%;
      height: 60px;
      background: #f8fafc;
      border: 2px solid #f1f5f9;
      border-radius: 18px;
      padding: 0 20px;
      outline: none;
      margin-bottom: 15px;
      font-weight: 600;
      font-size: 15px;
      transition: 0.3s;
      color: var(--dark);
    }

    .form-group input:focus {
      border-color: var(--p);
      background: white;
    }

    .btn-auth {
      width: 100%;
      height: 60px;
      background: var(--p);
      color: white;
      border: none;
      border-radius: 18px;
      font-weight: 800;
      font-size: 16px;
      cursor: pointer;
      box-shadow: 0 10px 20px rgba(255, 138, 26, 0.2);
      margin-top: 10px;
      transition: 0.3s;
    }

    .btn-auth:hover {
      background: var(--p-dark);
      transform: translateY(-2px);
    }
    /* --- FOOTER (MATCHED STYLE) --- */
    .premium-footer {
      background: #07133a;
      color: #f8fafc;
      margin-top: 120px;
      padding: 72px 0 0;
      position: relative;
    }

    .footer-container {
      max-width: 1300px;
      margin: 0 auto;
      padding: 0 40px;
    }

    .footer-main {
      display: grid;
      grid-template-columns: 2fr 1fr 1fr 1.25fr;
      gap: 42px;
      padding-bottom: 56px;
    }

    .footer-col h3 {
      font-size: 36px;
      font-size: clamp(26px, 3.2vw, 40px);
      line-height: 1;
      font-weight: 800;
      margin: 0 0 12px;
      letter-spacing: -0.6px;
    }

    .footer-logo {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      text-decoration: none;
      margin-bottom: 16px;
    }

    .footer-logo .logo-text {
      color: #ffffff;
      font-size: 44px;
      font-size: clamp(30px, 4vw, 52px);
      line-height: 1;
      font-weight: 800;
      letter-spacing: -1px;
    }

    .footer-desc {
      color: #9eb0d9;
      line-height: 1.4;
      font-size: 19px;
      max-width: 520px;
      margin: 0 0 22px;
    }

    .footer-col ul {
      list-style: none;
      margin: 0;
      padding: 0;
    }

    .footer-col ul li {
      margin-bottom: 12px;
    }

    .footer-col ul li a {
      color: #b4c1e0;
      text-decoration: none;
      font-size: 34px;
      font-size: clamp(20px, 2.1vw, 34px);
      line-height: 1.15;
      font-weight: 500;
      transition: color 0.2s ease;
    }

    .footer-col ul li a:hover {
      color: #ffffff;
    }

    .social-pills {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .social-pill {
      width: 46px;
      height: 46px;
      background: rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 12px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      color: #f8fafc;
      text-decoration: none;
      font-size: 15px;
      font-weight: 700;
      transition: background 0.2s ease, color 0.2s ease;
    }

    .social-pill:hover {
      background: #ff921f;
      color: #07133a;
    }

    .news-box h4 {
      margin: 0 0 14px;
      color: #ffffff;
      font-size: 38px;
      font-size: clamp(22px, 2.4vw, 38px);
      line-height: 1.1;
      font-weight: 800;
    }

    .news-field {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: 7px;
      border-radius: 16px;
      border: 1px solid rgba(255, 255, 255, 0.12);
      background: rgba(255, 255, 255, 0.07);
      max-width: 420px;
    }

    .news-field input {
      height: 46px;
      flex: 1;
      border: none;
      outline: none;
      color: #ffffff;
      font-size: 20px;
      padding: 0 14px;
      background: transparent;
    }

    .news-field input::placeholder {
      color: #97a7ca;
    }

    .news-field button {
      height: 46px;
      min-width: 88px;
      border: none;
      border-radius: 12px;
      background: #ff921f;
      color: #ffffff;
      font-size: 30px;
      font-size: clamp(16px, 1.8vw, 30px);
      font-weight: 700;
      padding: 0 18px;
      cursor: pointer;
      transition: filter 0.2s ease;
    }

    .news-field button:hover {
      filter: brightness(1.08);
    }

    .footer-bottom {
      border-top: 1px solid rgba(255, 255, 255, 0.12);
      padding: 26px 0 34px;
      text-align: center;
    }

    .footer-bottom p {
      margin: 0;
      color: #9fb0d5;
      font-size: 14px;
      font-weight: 500;
    }

    .to-top-btn {
      position: absolute;
      right: 26px;
      bottom: 108px;
      width: 50px;
      height: 50px;
      border-radius: 50%;
      background: #ff921f;
      color: #ffffff;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-size: 22px;
      font-weight: 800;
      box-shadow: 0 10px 24px rgba(255, 146, 31, 0.35);
    }

    @media (max-width: 1100px) {
      .footer-main {
        grid-template-columns: 1fr 1fr;
      }
    }

    @media (max-width: 700px) {
      .footer-container {
        padding: 0 20px;
      }

      .footer-main {
        grid-template-columns: 1fr;
        gap: 28px;
      }

      .footer-col ul li a {
        font-size: 22px;
      }

      .footer-desc {
        font-size: 17px;
      }

      .to-top-btn {
        right: 20px;
        bottom: 95px;
      }
    }

    /* ===== MOBILE APP UI (Native Feel) ===== */
    .mobile-app {
      display: none;
      background: #f7f8fa;
      min-height: 100vh;
      padding-bottom: 90px; /* Space for bottom nav */
    }

    .m-header {
      position: sticky;
      top: 0;
      background: var(--p);
      color: white;
      padding: 15px 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      z-index: 900;
      border-bottom-left-radius: 20px;
      border-bottom-right-radius: 20px;
      box-shadow: 0 10px 20px rgba(255, 138, 26, 0.2);
    }

    .m-location {
      font-weight: 700;
      display: flex;
      align-items: center;
      gap: 6px;
      font-size: 14px;
    }

    .m-avatar {
      width: 38px;
      height: 38px;
      border-radius: 12px;
      background: rgba(255, 255, 255, 0.2);
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 800;
      backdrop-filter: blur(5px);
      cursor: pointer;
    }

    .m-search {
      display: flex;
      gap: 10px;
      padding: 20px;
      background: white;
      margin: 15px;
      border-radius: 20px;
      box-shadow: var(--shadow-sm);
    }

    .m-search input {
      flex: 1;
      padding: 10px;
      border: none;
      outline: none;
      font-size: 15px;
      font-weight: 600;
      color: var(--dark);
    }

    .m-btn {
      background: var(--p);
      color: white;
      border: none;
      padding: 0 20px;
      border-radius: 12px;
      font-weight: 700;
      cursor: pointer;
    }

    .m-store-grid {
      padding: 0 15px;
      display: grid;
      grid-template-columns: 1fr;
      gap: 15px;
    }

    /* Styling for store cards in mobile view specifically */
    .m-store-grid .store-card {
      border-radius: 20px;
      box-shadow: var(--shadow-sm);
      border: none;
    }

    .m-bottom-nav {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      background: white;
      display: flex;
      justify-content: space-around;
      padding: 12px 0 20px 0; /* Extra padding for bottom safety area */
      border-top: 1px solid #f1f5f9;
      box-shadow: 0 -5px 20px rgba(0,0,0,0.03);
      z-index: 1000;
      border-top-left-radius: 25px;
      border-top-right-radius: 25px;
    }

    .m-bottom-nav button {
      background: none;
      border: none;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 4px;
      color: var(--gray);
      font-size: 20px;
      cursor: pointer;
      width: 60px;
    }

    .m-bottom-nav button span:last-child {
      font-size: 10px;
      font-weight: 700;
    }

    .m-bottom-nav button:hover, 
    .m-bottom-nav button.active {
      color: var(--p);
    }

    /* --- RESPONSIVE LOGIC --- */
    @media (max-width: 768px) {
      .nav-container,
      .hero,
      .main-content,
      .premium-footer {
        display: none !important;
      }

      .mobile-app {
        display: block;
      }

      body {
        background: #f7f8fa;
      }
    }
    .mobile-app {
  display: none;
}

@media (max-width: 768px) {

  .navbar-wrap,
  .main-content,
  .premium-footer {
    display: none !important;
  }

  .mobile-app {
    display: block !important;
    max-width: 480px;
    margin: auto;
    background: #fff;
    min-height: 100vh;
    padding-bottom: 70px;
  }
}

  </style>
</head>

<body id="top">

  <div class="nav-container">
    <nav class="navbar">
      <div style="display: flex; align-items: center; gap: 30px;">
        <a href="#" class="logo-box">
          <div class="logo-icon">L</div>
          <span class="logo-text">LocalBasket</span>
        </a>
        <div style="display: flex; align-items: center; gap: 10px; background: rgba(248, 250, 252, 0.8); padding: 8px 15px; border-radius: 15px; cursor: pointer; border: 1px solid #f1f5f9;" id="locBtn">
          <span style="font-size: 18px;">ðŸ“</span>
          <div style="display: flex; flex-direction: column;">
            <span style="font-size: 10px; font-weight: 800; color: var(--p); text-transform: uppercase; letter-spacing: 0.5px;">Deliver to</span>
            <span id="locText" style="font-size: 13px; font-weight: 700; color: var(--dark);">Searching Location...</span>
          </div>
        </div>
      </div>

      <div class="nav-actions">
        <a href="/seller.html" class="sell-btn">
          <span>Store Dashboard</span>
          <span class="sell-badge">Sell on LB</span>
        </a>

        <div class="cart-pill">
          ðŸ›’ <span id="cartCount">0 Items</span>
        </div>

        <div class="user-account">
          <div class="user-profile-trigger" id="accountBtn">
            <div class="user-avatar">
              <span id="userInitials">U</span>
            </div>
          </div>

          <div class="user-menu" id="userMenu" style="display:none;">
            <p id="userFullName">User</p>
            <button onclick="viewProfile()">View Profile</button>
            <button onclick="logoutUser()">Logout</button>
          </div>
        </div>
      </div>

    </nav>
  </div>

  <header class="hero">
    <h1>Your Neighborhood,<br><span>Now on Speed Dial.</span></h1>
    <p>Direct from local stores to your door in 20 minutes. No warehouses, just community.</p>
    <div class="search-wrapper">
      <input type="text" placeholder="Enter your pincode for local shops..." id="pinInput">
      <button class="btn-find">Find Shops</button>
    </div>
    <div class="categories">
      <div class="chip active">All Stores</div>
      <div class="chip">ðŸ¥¬ Groceries</div>
      <div class="chip">ðŸ’Š Pharmacy</div>
      <div class="chip">ðŸ° Bakery</div>
      <div class="chip">ðŸ§´ Personal Care</div>
    </div>
  </header>

  <main class="main-content">
    <div class="section-header">
      <div>
        <h2 class="section-title">Nearby Partners</h2>
        <p class="section-subtitle" id="locationLabel">
          Verified stores near you
        </p>
      </div>
      <button class="map-btn" id="viewOnMapBtn">
        View On Map â†—
      </button>
    </div>

    <div class="store-grid" id="storeGrid">
      </div>

    <div class="empty-state" id="emptyState" style="display:none;">
      <p>No stores found in your area.</p>
    </div>
  </main>

    <footer class="premium-footer">
    <div class="footer-container">
      <div class="footer-main">
        <div class="footer-col">
          <a href="#" class="footer-logo">
            <div class="logo-icon">LB</div>
            <span class="logo-text">LocalBasket</span>
          </a>
          <p class="footer-desc">Your neighborhood, digitalized. Order fresh groceries from trusted local stores.</p>
          <div class="social-pills">
            <a href="#" class="social-pill" aria-label="Website">WEB</a>
            <a href="#" class="social-pill" aria-label="Instagram">IG</a>
            <a href="#" class="social-pill" aria-label="X">X</a>
            <a href="#" class="social-pill" aria-label="LinkedIn">IN</a>
          </div>
        </div>

        <div class="footer-col">
          <h3>Company</h3>
          <ul>
            <li><a href="#">About Us</a></li>
            <li><a href="#">Careers</a></li>
            <li><a href="#">Blog</a></li>
            <li><a href="#">Press</a></li>
          </ul>
        </div>

        <div class="footer-col">
          <h3>Support</h3>
          <ul>
            <li><a href="#">Help Center</a></li>
            <li><a href="#">Privacy Policy</a></li>
            <li><a href="#">Terms of Service</a></li>
            <li><a href="#">Refund Policy</a></li>
          </ul>
        </div>

        <div class="footer-col">
          <div class="news-box">
            <h4>Stay in loop</h4>
            <div class="news-field">
              <input type="email" placeholder="Email address">
              <button>Join</button>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="footer-bottom">
      <div class="footer-container">
        <p>&copy; 2026 LocalBasket &middot; Built in India</p>
      </div>
    </div>
    <a href="#top" class="to-top-btn" aria-label="Back to top">&#8593;</a>
  </footer>

  <div class="auth-overlay" id="authOverlay" style="display:none;">
    <div class="auth-card">

      <button class="auth-close-btn" onclick="toggleAuth(false)">Ã—</button>

      <div class="auth-tabs">
        <button class="auth-tab-btn active" onclick="switchTab('login')">Login</button>
        <button class="auth-tab-btn" onclick="switchTab('register')">Sign Up</button>
      </div>

      <div class="form-group">

        <div id="registerFields" style="display:none;">
          <input type="text" id="regName" placeholder="Full Name">
          <input type="email" id="regEmail" placeholder="Email Address">
        </div>

        <input type="number" id="authPhone" placeholder="Phone Number">
        <input type="password" id="authPassword" placeholder="Password">

        <button class="btn-auth" onclick="submitAuth()">Continue</button>
      </div>

    </div>
  </div>


  <script src="welcome.js"></script>
</body>

</html>

